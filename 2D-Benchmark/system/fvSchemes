/*--------------------------------*- C++ -*----------------------------------*\
| =========                 |                                                 |
| \\      /  F ield         | OpenFOAM: The Open Source CFD Toolbox           |
|  \\    /   O peration     | Version:  v2106                                 |
|   \\  /    A nd           | Website:  www.openfoam.com                      |
|    \\/     M anipulation  |                                                 |
\*---------------------------------------------------------------------------*/
FoamFile
{
    version     2.0;
    format      ascii;
    class       dictionary;
    object      fvSchemes;
}
// * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * //

//divScheme Gauss WENOUpwindFit 2 1;
divScheme Gauss linear;

ddtSchemes
{
    default         Euler;
}

gradSchemes
{
    default         cellLimited Gauss linear 1;
    grad(U)         cellLimited Gauss linear 1;
}

divSchemes
{
    div(rhoPhi,U)           $divScheme;

    "div\(alphaPhi.*,Yi\)"  $divScheme;

    "div\(phi,alpha.*\)"    $divScheme;
    "div\(phir,alpha.*\)"   $divScheme;

    "div\(.*1.*\)"          $divScheme;

    DDt(phi,alpha)          $divScheme;

    div(((rho*nuEff)*dev2(T(grad(U))))) $divScheme;

    div(phi,T)              $divScheme;

    div(HbyA)               $divScheme;

}

laplacianSchemes
{
    //default             Gauss linear orthogonal;
    default               Gauss linear limited 1;
}

interpolationSchemes
{
    default         linear;
}

snGradSchemes
{
    default         orthogonal;
    //default               Gauss linear limited 1;
}

fluxRequired
{
    default         no;
    p_rgh             ;
    "alpha.*"         ;
}


// ************************************************************************* //
